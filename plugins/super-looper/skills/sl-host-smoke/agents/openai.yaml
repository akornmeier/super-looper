interface:
  display_name: "Host Compatibility Smoke Test"
  short_description: "Verify Claude and Codex runtime adapters"
  default_prompt: "Use $sl-host-smoke to verify this plugin host setup."
policy:
  allow_implicit_invocation: false
