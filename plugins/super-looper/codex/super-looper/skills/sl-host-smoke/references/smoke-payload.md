# Shared smoke payload

- `reference_marker`: `shared-reference-v1`
- Question: "Which compatibility path should this diagnostic exercise?"
- Options: `Alpha` -> `alpha`; `Beta` -> `beta`

Use the selected value unchanged in the script and worker markers.
