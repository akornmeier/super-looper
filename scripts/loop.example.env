#!/usr/bin/env bash
#
# loop.example.env — a documented, runnable example invocation of the loop
# driver. Copy it, edit the variables for your throwaway target, then run:
#
#     bash scripts/loop.example.env
#
# The headless `claude` flags are NOT respecified here: they are pinned inside
# scripts/loop.sh (the single source) so this example and the driver can never
# diverge. For the record, loop.sh launches the run as:
#
#     env -i HOME=... PATH=... [GH_TOKEN=...] \
#       claude -p "<lfg routing prompt + seed, OR plan-routing prompt naming plan:<path>>" \
#         --plugin-dir <plugin> --model <model> --dangerously-skip-permissions
#
# (Plan-input mode — loop.sh --plan-file — names a committed plan via the literal
# plan:<path> marker so lfg skips planning; see docs/loop-driver.md.)
#
# Confirm the exact constructed command at any time with `--dry-run` (below).
#
# NOTE (execution-time unknown): the precise headless trigger form — these inline
# routing prompts vs. a `/lfg` slash command, and long multi-turn behavior — is
# pinned by the live acceptance smoke (docs/loop-driver-acceptance.md). If that
# smoke changes the form, change it in loop.sh (LOOP_PROMPT_PREFIX /
# LOOP_PLAN_PROMPT_PREFIX) only; this file needs no edit.

set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"

# --- Edit these for your throwaway target ------------------------------------
TARGET="${TARGET:-/absolute/path/to/throwaway-repo}"   # must be OUTSIDE this plugin checkout
SEED_FILE="${SEED_FILE:-$HERE/../examples/loop-seed.md}"
MODEL="${MODEL:-opus}"          # opus or fable
TIMEOUT="${TIMEOUT:-1800}"      # per-attempt wall-clock seconds
MAX_RETRIES="${MAX_RETRIES:-2}" # re-launches after a crash-without-DONE
LOG_DIR="${LOG_DIR:-/tmp/super-looper/loop}" # run-log dir; loop.sh and the append below must agree

# --- Verification mode (pick ONE) --------------------------------------------
# Faithful GitHub-CI bar (recommended for sign-off): leave --verify-cmd off and
# give the throwaway a remote + the sample Actions workflow. loop.sh then gates
# success on an open PR with green `gh pr checks`.
#
# Local proxy (no Actions needed): append  --verify-cmd bun test
# to the invocation below to prove the mechanism without standing up CI.

# Print the exact command first (no execution), then run it for real.
bash "$HERE/loop.sh" \
  --target "$TARGET" \
  --seed-file "$SEED_FILE" \
  --model "$MODEL" \
  --timeout "$TIMEOUT" \
  --max-retries "$MAX_RETRIES" \
  --log-dir "$LOG_DIR" \
  --dry-run

# Capture the real run's exit so the append still runs on a FAILED loop. loop.sh
# exits non-zero on cap-exhausted/timeout/done-but-red (5/6/7), and those failure
# records belong in the metric's denominator — promoting them is the whole point.
# Without the `|| LOOP_EXIT=$?` capture, `set -e` aborts before the append.
LOOP_EXIT=0
bash "$HERE/loop.sh" \
  --target "$TARGET" \
  --seed-file "$SEED_FILE" \
  --model "$MODEL" \
  --timeout "$TIMEOUT" \
  --max-retries "$MAX_RETRIES" \
  --log-dir "$LOG_DIR" || LOOP_EXIT=$?

# --- Promote the run-record into the committed ledger ------------------------
# Append the newest run-record (success OR failure) to docs/run-records/ledger.jsonl,
# where the pulse reads it. A no-op when LOG_DIR holds no records.
bash "$HERE/append-run-record.sh" --log-dir "$LOG_DIR"

exit "$LOOP_EXIT"
