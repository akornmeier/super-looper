{
  "name": "super-looper",
  "version": "0.1.0",
  "description": "Super Looper plugin for Claude Code",
  "type": "module",
  "private": true,
  "homepage": "https://github.com/akornmeier/super-looper",
  "repository": "https://github.com/akornmeier/super-looper",
  "scripts": {
    "test": "bun test",
    "plugin:validate": "claude plugin validate .claude-plugin/marketplace.json && claude plugin validate plugins/super-looper",
    "release:preview": "bun run scripts/release/preview.ts",
    "release:sync-metadata": "bun run scripts/release/sync-metadata.ts --write",
    "release:validate": "bun run scripts/release/validate.ts"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/js-yaml": "^4.0.9",
    "bun-types": "^1.0.0",
    "semantic-release": "^25.0.3"
  }
}
