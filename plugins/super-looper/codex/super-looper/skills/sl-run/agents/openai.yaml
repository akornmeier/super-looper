interface:
  display_name: "Run Plan"
  short_description: "Route and run a code-owned plan to review-ready"
  default_prompt: "Use $sl-run to select the least expensive safe workflow profile, record isolation, and execute this plan through deterministic checks and independent verification."
policy:
  allow_implicit_invocation: true
