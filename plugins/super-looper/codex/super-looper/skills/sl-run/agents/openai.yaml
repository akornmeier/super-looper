interface:
  display_name: "Run Plan"
  short_description: "Run a code-owned plan workflow to review-ready"
  default_prompt: "Use $sl-run to execute this plan through deterministic checks and independent verification."
policy:
  allow_implicit_invocation: true
