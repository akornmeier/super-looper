# sl-work compatibility evals

Use the `skill-creator` fresh-source workflow. Inject the current `sl-work/SKILL.md` and only the reference needed by the case; do not invoke the session-cached plugin skill.

The suite checks Markdown plan routing, bare-description planning, durable state resume, older unattended-call preservation, and the isolated HTML compatibility boundary. Kernel behavior remains covered by the `sl-run` suite.
